public class Main{
    public static void main(String[] args){
        // String = a reference data type that can store one or more characters reference data types have
        // access to useful methods

        String name = "Hello world";
        // boolean result = name.equals("Hello world"); // True
        // int result = name.length(); // 11
        // char result = name.charAt(0); // H
        // int result = name.indexOf("o"); // 4
        // String result = name.toUpperCase(); // "HELLO WORLD"
        // String result = name.toLowerCase(); // "hello world"
        // String result = name.trim(); // trim method will remove the extra empty spaces
        // String result = name.replace("o", "y") // Replace method will all the o char with y char
        System.out.println(result);

        String name1 = "";
        boolean result1 = name.isEmpty();
        System.out.println(result1);
    }
}